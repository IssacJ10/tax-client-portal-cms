HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Google reCAPTCHA v3 (Optional - for bot protection)
# Get your keys from: https://www.google.com/recaptcha/admin
# 1. Create a new site with reCAPTCHA v3
# 2. Add your domains (localhost for development)
# 3. Copy the Secret Key (private) here
JJ_PORTAL_CAPTCHA_SECRET=

# reCAPTCHA score threshold (0.0 to 1.0)
# Lower = more permissive, Higher = more strict
# Default: 0.5 (Google's recommended starting point)
JJ_PORTAL_CAPTCHA_MIN_SCORE=0.5

# ===========================================
# Email Configuration
# ===========================================
# Supported providers: google, sendgrid, brevo, ses
EMAIL_PROVIDER=google

# Google Workspace SMTP
# 1. Go to Google Admin Console > Security > 2-Step Verification (enable if not)
# 2. Go to myaccount.google.com > Security > 2-Step Verification > App passwords
# 3. Generate an App Password for "Mail" on "Other (Custom name)"
# 4. Use the 16-character password below (no spaces)
GOOGLE_SMTP_USER=noreply@yourdomain.com
GOOGLE_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Email sender settings
SMTP_FROM=noreply@yourdomain.com
SMTP_REPLY_TO=support@yourdomain.com

# Alternative: SendGrid (100 emails/day free)
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=SG.xxxx

# Alternative: Brevo (300 emails/day free)
# EMAIL_PROVIDER=brevo
# BREVO_USER=your-brevo-email
# BREVO_API_KEY=xkeysib-xxxx
